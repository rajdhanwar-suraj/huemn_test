import React from 'react';
import './MainContent.css';

const MainContent = () => {
  return (
    <main>
      <section className="hero">
        <h1>Get more done with the new Google Chrome</h1>
        <p>Now more simple, secure, and faster than ever - with Google's smarts built-in.</p>
        <a href="#download" className="download-button">Download Chrome</a>
      </section>
      {/* Add more sections as needed */}
    </main>
  );
};

export default MainContent;
